# 改修仕様書 05

## フェーズ1.5 - zaicoClient.js完全移行

### 改修の目的
zaicoClient.jsへの完全移行とAPI呼び出しの統一

### 改修内容

#### 1. zaicoClient.jsの拡張
- 全Zaico API操作の統合
- 統一されたインターフェース提供

#### 2. API呼び出し関数の統合
```javascript
// zaicoClient.js
export const zaicoClient = {
  getInventories: () => fetch('/api/zaico/inventories'),
  updateInventory: (id, data) => fetch(`/api/zaico/inventories/${id}`, {
    method: 'PUT',
    body: JSON.stringify(data)
  }),
  // その他のAPI操作
}
```

#### 3. 全コンポーネントの移行
- zaicoApi.js使用箇所をzaicoClient.jsに変更
- import文の更新

### 影響範囲
- `src/utils/zaicoClient.js`
- Zaico APIを使用する全コンポーネント

### 所要時間
3-4時間

### 検証項目
- 全てのZaico API機能が正常動作すること
- zaicoApi.jsが不要になっていること
- エラーハンドリングが統一されていること

### 次のステップ
改修仕様書_06: ApiKeyCheckerコンポーネント修正