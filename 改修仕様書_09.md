# 改修仕様書 09

## フェーズ1.9 - パスワードハッシュ化ライブラリ導入

### 改修の目的
パスワードのハッシュ化に必要なライブラリを導入する

### 改修内容

#### 1. bcryptライブラリの導入
```json
{
  "dependencies": {
    "bcryptjs": "^2.4.3"
  }
}
```

#### 2. パスワードハッシュ化ユーティリティの作成
```javascript
// src/utils/passwordHash.js
import bcrypt from 'bcryptjs';

export const hashPassword = async (password) => {
  const saltRounds = 10;
  return await bcrypt.hash(password, saltRounds);
}

export const verifyPassword = async (password, hashedPassword) => {
  return await bcrypt.compare(password, hashedPassword);
}
```

#### 3. 設定とテスト
- ハッシュ化強度の設定
- 基本的な動作テスト

### 影響範囲
- `package.json`
- 新規: `src/utils/passwordHash.js`

### 所要時間
1時間

### 検証項目
- bcryptライブラリが正常にインストールされること
- ハッシュ化・検証機能が正常動作すること
- パフォーマンスが許容範囲内であること

### 次のステップ
改修仕様書_10: AuthContextパスワードハッシュ化対応