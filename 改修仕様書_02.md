# 改修仕様書 02

## フェーズ1.2 - APIキー暗号化機能の実装

### 改修の目的
APIキーを安全に暗号化して保存・管理する

### 改修内容

#### 1. 暗号化ライブラリの導入
```json
{
  "dependencies": {
    "crypto": "^1.0.1"
  }
}
```

#### 2. 暗号化ユーティリティの作成
- `src/utils/encryption.js`の作成
- AES暗号化/復号化関数
- 環境固有の暗号化キー生成

#### 3. APIキー暗号化機能
```javascript
// encryption.js
export const encryptApiKey = (apiKey) => { /* 実装 */ }
export const decryptApiKey = (encryptedKey) => { /* 実装 */ }
```

### 影響範囲
- 新規: `src/utils/encryption.js`
- `package.json`

### 所要時間
2-3時間

### 検証項目
- 暗号化・復号化が正常に動作すること
- 暗号化されたデータが平文で読み取れないこと

### 次のステップ
改修仕様書_03: バックエンドAPIルート強化